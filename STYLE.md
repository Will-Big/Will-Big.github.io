# 글쓰기 기준

이 저장소의 페이지 본문을 쓸 때 지키는 기준입니다. 디자인이 아니라 **문장과 제목, 구성**에 대한
규칙입니다. 새 페이지를 쓸 때와 기존 페이지를 고칠 때 모두 적용합니다.

---

## 문장

### 1. 과하게 끊지 않는다

한 가지 사실을 짧은 단정문 두세 개로 쪼개 호흡을 만들지 않습니다. 이어지는 문장이 같은 대상을
설명하고 있으면 접속해서 하나로 만듭니다.

> **전** 헤드리스 테스트는 SO가 아니라 그렇게 생성된 코드를 읽었습니다. 저작은 Unity에서 하고
> 검증은 코어에서 하려면 둘을 잇는 다리가 필요했고, 이 생성기가 그 다리였습니다. 카드 데이터의
> 원본이 두 곳으로 나뉜 것도 이 지점입니다.
>
> **후** 헤드리스 테스트는 위 저작 과정에서 생성된 코드를 사용했습니다. 이 과정에는 반드시 SO가
> 포함되어야 했고, 이로 인해 같은 데이터가 SO와 리터럴 코드로 병존하는 상황이 발생했습니다.

### 2. 되풀이하는 마무리 문장을 붙이지 않는다

이미 말한 것을 다시 요약하거나 의미를 덧대는 문장을 지웁니다. 남겨야 한다면 앞 문장에 접속해
흡수시킵니다.

> 지우는 예 — "지운 구조가 다시 들어오지 않게 하려는 조항입니다." ·
> "그리고 받은 구조는 받은 자리에서 한 번 더 확인해야 합니다." ·
> "그 기간을 테스트로 묶어 두고 짧게 끝내려던 것입니다."

### 3. 극적으로 운을 떼지 않는다

짧게 던지고 줄표로 결론을 끄는 구성을 쓰지 않습니다. 자기 지시적인 안내문도 넣지 않습니다.

| 쓰지 않는 것 | 대신 |
|---|---|
| "대가는 분명했습니다 — 코어는 …" | "이 구조에서는 코어가 …" |
| "아래 첫 번째 사례가 여기서 시작합니다" | 사례가 바로 이어지므로 안내하지 않는다 |

### 4. 격식 있는 문어체를 유지하되 늘이지 않는다

합니다체로 쓰고 구어체와 반말을 쓰지 않습니다. 격식을 이유로 수식을 덧붙여 장황해지지 않게 합니다.

**예외 — 표와 카드의 좁은 셀.** `왜` · `무엇이 나아졌나` 같은 칸과 비교표 셀은 명사형으로 끊어도
됩니다. 폭이 좁아 `~습니다` 가 줄을 하나 더 만들기 때문입니다.

> "컴포넌트가 늘 때마다 에디터를 고쳐야 해서 기획 요청을 제때 받아주지 못함"
> "루프에 로직을 계속 덧붙이는 구조라 확장이 막힘"

**본문 문단에는 쓰지 않습니다.** 셀 밖으로 나온 글은 예외 없이 합니다체입니다.

---

## 제목

### 5. 문장이 아니라 명사구로 쓴다

| 문장형 (쓰지 않음) | 명사구 |
|---|---|
| 제가 말하지 않은 것 | 전환 전 예정하고 있던 방식 |
| 지우는 일을 마지막에 두었습니다 | 전환 순서 |
| 문서를 두 층으로 나눴습니다 | 설계 문서의 이층 구조 |
| 왜 보지 않았는가 | 검토가 이루어지지 않은 원인 |

**예외 — 심화 페이지의 표제.** 한 페이지 전체가 하나의 주장을 증명하는 글이라면, `h1` 은 그 주장을
서술문으로 적을 수 있습니다. 명사구로 바꾸면 증명할 것이 사라지고 목차 항목처럼 보이기 때문입니다.
절 제목(`h2` · `h3`)에는 적용하지 않습니다.

> `코어는 자기가 어떻게 실행되는지 모른다` · `수치 하나를 고치려고 컴파일하지 않는다` ·
> `기능은 컴포넌트에, 컴포넌트는 오브젝트에 위임한다`

### 6. 제목에 `-` 를 쓰지 않는다

고유명사(`Rogue-deck`)만 예외입니다. 부제가 필요하면 괄호로 답니다.

> `설계 개요 — 사람 검수용` → `설계 개요 (사람 검수용)`

라벨과 값을 가르는 줄표는 여기에 해당하지 않습니다. `역할 — 세 번째 자체 엔진 · 에디터` 처럼
칸 이름 뒤에 오는 줄표는 그대로 씁니다.

### 7. 변명처럼 읽히거나 앞과의 관계를 잃지 않게 한다

"제가 말하지 않은 것"은 *깜빡했다*는 뉘앙스를 줍니다. 반대로 "현재의 저작 경로"는 결과만 가리켜
그것이 무엇의 결과인지를 잃습니다. 제목은 그 절이 **무엇을 다루는지와 무엇으로부터 왔는지**를
함께 가리켜야 합니다.

> `현재의 저작 경로` → `두 사례를 기반으로 한 저작 구조의 개선`

---

## 구성

### 8. 전제를 문제보다 먼저 놓는다

배경과 의도, 예정하고 있던 것을 문제 서술 앞에 둡니다. 그래야 읽는 사람이 "이것 때문에 저 문제가
생겼구나"를 따라옵니다. 무엇을 예정했는지와 무엇이 반영되지 않았는지는 **서로 대응되게** 적어,
어긋난 지점을 바로 확인할 수 있게 합니다.

### 9. 필요하지 않은 정보를 넣지 않는다

구현 트리비아나 클래스 이름 나열처럼 판단을 나르지 않는 것은 뺍니다. 기준은 하나입니다 —
**한 문장을 지웠을 때 판단이 덜 전달되지 않으면 지웁니다.**

### 10. 보여야 할 것은 프로그래머의 판단이다

앞으로의 콘텐츠 계획이나 기획 로드맵은 넣지 않습니다. 진행 상태는 배지 같은 표시로 대신합니다.

### 11. 원인을 외부 주체 탓으로 쓰지 않는다

AI 협업을 다루는 대목에 적용합니다. "AI가 만든 구조로 손해를 봤다" 같은 서술 대신 자신의 판단과
책임으로 기술합니다.
